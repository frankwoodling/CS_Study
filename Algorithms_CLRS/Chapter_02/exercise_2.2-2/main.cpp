// CLRS Exercise
// 2.2-2
// Pseudocode given a description of a selection sort algorithm


/*

A= {};
key = -1;
key_index = -1;

for i = 1:length(A)
    key = min(i+1:length(A))
    key_index = i of min

    A[i] = A[key_index]
    A[key_index] = key


 */

