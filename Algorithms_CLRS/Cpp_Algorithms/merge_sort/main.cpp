// Merge-sort
// Incomplete
#include <iostream>
#include <vector>

using namespace std;

vector<int> merge_sort(vector<int> A, int p, int r){
    q = (p + r)/2;
    merge_sort(A, p, q);

}

vector merge(A, p, q, r){

}

int main(){
    int A = 1
}